# Лабораторная работа №6

Система обработки задач с использованием объектно-ориентированного подхода и автоматизированного тестирования.

---

### Описание
Проект представляет собой модуль для работы с задачами (Tasks), загружаемыми из внешних JSON-источников. Реализована валидация данных через `dataclasses`, кастомная итерация и безопасная работа с файловой системой.

### Функционал
* Использование `@dataclass` для типизации и структуры задач.
* Классы `TaskSource` для чтения задач из файлов, API и генераторов.
* Класс `TaskProcessor` для обработки задач из источников `TaskSource`.
* Тестирование, включая отсутствие файлов и ошибки парсинга, с использованием `pytest`.

### Структура проекта
```
.
├── src/
│   ├── __init__.py
│   ├── constants.py             # Константы
│   ├── logger.py                # Логирование
│   ├── main.py                  # Точка входа
│   ├── task.py                  # Класс задачи, протокол источника задач
│   ├── task_process.py          # Класс для обработчки задач из источников
│   └── task_sources.py          # Классы источников задач с реализацией протокола
├── tests/
│   ├── __init__.py
│   └── test_task_process.py     # Модульные тесты (pytest)
├── .gitignore                   # Файл .gitignore
├── .pre-commit-config.yaml      # Конфиг .pre-commit
├── pyproject.toml               # Конфиг проекта, настройки pytest-cov
├── example.json                 # Пример структуры данных
└── README.md                    # Этот файл
```
